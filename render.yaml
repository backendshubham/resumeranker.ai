services:
  - type: web
    name: your-app-name
    env: node
    buildCommand: npm install
    startCommand: npm start
    plan: free
    branches:
      - main
    envVars:
      - key: NODE_ENV
        value: production
